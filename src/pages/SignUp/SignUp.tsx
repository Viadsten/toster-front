import { FC } from "react";
import { SignUpProps } from ".";
import { UiButton } from "@/shared/ui/UiButton";

export const SignUp: FC<SignUpProps> = (props) => {
  return (
    <div>
      <UiButton>Ширнуться</UiButton>
    </div>
  );
};
